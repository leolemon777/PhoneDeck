<Window x:Class="PhoneDeck.ControlCenter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="PhoneDeck 电脑控制台"
        Icon="Assets/PhoneDeck.Light.ico"
        Width="1260" Height="900" MinWidth="640" MinHeight="560"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="{StaticResource FontFamilyBody}"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        RenderOptions.ClearTypeHint="Enabled"
        RenderOptions.BitmapScalingMode="HighQuality">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="64" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="StatusValue" TargetType="TextBlock" BasedOn="{StaticResource TextBase}">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,10,0,4" />
        </Style>
        <Style x:Key="NodeTitle" TargetType="TextBlock" BasedOn="{StaticResource TextBase}">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
        <Style x:Key="NodeMeta" TargetType="TextBlock" BasedOn="{StaticResource TextFine}">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,3,0,0" />
        </Style>
        <Style x:Key="SettingsTitle" TargetType="TextBlock" BasedOn="{StaticResource TextBase}">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="SettingsMeta" TargetType="TextBlock" BasedOn="{StaticResource TextFine}">
            <Setter Property="Margin" Value="0,3,0,0" />
        </Style>
        <Style x:Key="PageNavIcon" TargetType="Path">
            <Setter Property="Width" Value="14" />
            <Setter Property="Height" Value="14" />
            <Setter Property="Stretch" Value="Uniform" />
            <Setter Property="StrokeThickness" Value="1.7" />
            <Setter Property="Stroke" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
            <Setter Property="Margin" Value="0,0,7,0" />
        </Style>
    </Window.Resources>

    <Border x:Name="WindowRoot" Background="{DynamicResource BrushPearl}" BorderBrush="{DynamicResource BrushHairline}" BorderThickness="1" CornerRadius="20">
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Rectangle Grid.RowSpan="2" Height="540" VerticalAlignment="Top" Fill="{DynamicResource BrushAuroraGlow}" IsHitTestVisible="False" />

        <!-- Aether floating navigation and custom window chrome -->
        <Grid Grid.Row="0" Margin="24,12,18,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource PillGlass}" Padding="10,6" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image x:Name="BrandIcon" Width="32" Height="32" Source="Assets/PhoneDeck.Light.png" Stretch="Uniform" Margin="0,0,8,0" RenderOptions.BitmapScalingMode="HighQuality" />
                    <TextBlock Text="PhoneDeck" Style="{StaticResource TextBase}" FontWeight="SemiBold" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <Button x:Name="NavOverview" AutomationProperties.Name="概览" Style="{StaticResource NavButton}" Tag="Active" Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal"><Path Style="{StaticResource PageNavIcon}" Data="{StaticResource IconOverview}" /><TextBlock Text="概览" /></StackPanel>
                    </Button>
                    <Button x:Name="NavConnection" AutomationProperties.Name="连接" Style="{StaticResource NavButton}" Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal"><Path Style="{StaticResource PageNavIcon}" Data="{StaticResource IconLink}" /><TextBlock Text="连接" /></StackPanel>
                    </Button>
                    <Button x:Name="NavDevices" AutomationProperties.Name="设备" Style="{StaticResource NavButton}" Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal"><Path Style="{StaticResource PageNavIcon}" Data="{StaticResource IconPhone}" /><TextBlock Text="设备" /></StackPanel>
                    </Button>
                    <Button x:Name="NavLogs" AutomationProperties.Name="日志" Style="{StaticResource NavButton}" Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal"><Path Style="{StaticResource PageNavIcon}" Data="{StaticResource IconLogs}" /><TextBlock Text="日志" /></StackPanel>
                    </Button>
                    <Button x:Name="NavSettings" AutomationProperties.Name="设置" Style="{StaticResource NavButton}" Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal"><Path Style="{StaticResource PageNavIcon}" Data="{StaticResource IconSettings}" /><TextBlock Text="设置" /></StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Border Style="{StaticResource PillGlass}" Padding="14,8" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="7" Height="7" Fill="{DynamicResource BrushSuccess}" Margin="0,0,7,0" />
                        <TextBlock Text="本地控制 · 1.6" Style="{StaticResource TextFine}" VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
                <Button Style="{StaticResource WindowChromeButton}" Click="Minimize_Click"><Path Data="{StaticResource IconWindowMinimize}" Width="10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2" /></Button>
                <Button Style="{StaticResource WindowChromeButton}" Click="Maximize_Click"><Path Data="{StaticResource IconWindowMaximize}" Width="9" Height="9" Stretch="Fill" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.1" /></Button>
                <Button Style="{StaticResource WindowCloseButton}" Click="Close_Click"><Path Data="{StaticResource IconWindowClose}" Width="9" Height="9" Stretch="Fill" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2" /></Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <!-- Overview：小屏（&lt;900px 高）内容超出时允许纵向滚动，避免固定行高互相重叠 -->
            <ScrollViewer x:Name="PageOverview" VerticalScrollBarVisibility="Auto">
                <Grid Margin="32,12,32,26">
                <Grid.RowDefinitions>
                    <RowDefinition Height="84" />
                    <RowDefinition Height="112" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="164" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Top">
                        <TextBlock Text="PHONEDECK / 概览" FontFamily="{StaticResource FontFamilyMono}" FontSize="10.5" FontWeight="Bold" Foreground="{DynamicResource BrushSystemBlue}" />
                        <TextBlock Text="电脑控制台" Style="{StaticResource TextPageTitle}" Margin="0,5,0,0" />
                        <TextBlock Text="同一 Wi-Fi 自动连接 · 手机语音直达当前电脑" Style="{StaticResource TextMuted}" Margin="0,4,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,4,0,0">
                        <StackPanel Margin="0,0,10,0">
                            <Border x:Name="OverallBadge" Background="#EAFFFFFF" BorderBrush="{DynamicResource BrushHairline}" BorderThickness="1" CornerRadius="18" Padding="12,8">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="OverallDot" Width="7" Height="7" Fill="{DynamicResource BrushMist}" Margin="0,0,7,0" />
                                    <TextBlock x:Name="OverallText" Text="正在检查…" Style="{StaticResource TextCardTitle}" VerticalAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <TextBlock x:Name="LastCheckedText" Text="最后检查 —" Style="{StaticResource TextFine}" HorizontalAlignment="Right" Margin="0,4,2,0" />
                        </StackPanel>
                        <Button x:Name="StartButton" Content="启动接收端" Style="{StaticResource BlueButton}" Margin="0,0,8,0" Click="StartButton_Click" />
                        <Button x:Name="RestartButton" Content="重新启动" Style="{StaticResource GlassButton}" Margin="0,0,8,0" Click="RestartButton_Click" />
                        <Button x:Name="StopButton" Content="停止" Style="{StaticResource DangerGhostButton}" Click="StopButton_Click" />
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="16,13">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <Grid><TextBlock Text="接收端" Style="{StaticResource TextCardTitle}" /><Ellipse x:Name="ReceiverDot" Width="8" Height="8" Fill="{DynamicResource BrushMist}" HorizontalAlignment="Right" /></Grid>
                            <TextBlock x:Name="ReceiverValue" Grid.Row="1" Text="检查中" Style="{StaticResource StatusValue}" />
                            <TextBlock x:Name="ReceiverDetail" Grid.Row="2" Text="—" Style="{StaticResource TextFine}" TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </Border>
                    <Border Grid.Column="2" Style="{StaticResource GlassCard}" Padding="16,13">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <Grid><TextBlock Text="Wi-Fi" Style="{StaticResource TextCardTitle}" /><Ellipse x:Name="WifiDot" Width="8" Height="8" Fill="{DynamicResource BrushMist}" HorizontalAlignment="Right" /></Grid>
                            <TextBlock x:Name="WifiValue" Grid.Row="1" Text="检查中" Style="{StaticResource StatusValue}" />
                            <TextBlock x:Name="WifiDetail" Grid.Row="2" Text="—" Style="{StaticResource TextFine}" TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </Border>
                    <Border Grid.Column="4" Style="{StaticResource GlassCard}" Padding="16,13">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <Grid><TextBlock Text="手机音频" Style="{StaticResource TextCardTitle}" /><Ellipse x:Name="AudioDot" Width="8" Height="8" Fill="{DynamicResource BrushMist}" HorizontalAlignment="Right" /></Grid>
                            <TextBlock x:Name="AudioValue" Grid.Row="1" Text="检查中" Style="{StaticResource StatusValue}" />
                            <TextBlock x:Name="AudioDetail" Grid.Row="2" Text="—" Style="{StaticResource TextFine}" TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </Border>
                    <Border Grid.Column="6" Style="{StaticResource GlassCard}" Padding="16,13">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <Grid><TextBlock Text="USB 兜底" Style="{StaticResource TextCardTitle}" /><Ellipse x:Name="UsbDot" Width="8" Height="8" Fill="{DynamicResource BrushMist}" HorizontalAlignment="Right" /></Grid>
                            <TextBlock x:Name="UsbValue" Grid.Row="1" Text="检查中" Style="{StaticResource StatusValue}" />
                            <TextBlock x:Name="UsbDetail" Grid.Row="2" Text="—" Style="{StaticResource TextFine}" TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="1.65*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="22,18">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <Grid>
                                <StackPanel><TextBlock Text="连接拓扑" Style="{StaticResource TextSectionTitle}"/><TextBlock Text="当前设备 · 证书固定 HTTPS · 协议 v2" Style="{StaticResource TextFine}" Margin="0,3,0,0"/></StackPanel>
                                <Border HorizontalAlignment="Right" VerticalAlignment="Top" Background="#102DA44E" BorderBrush="#302DA44E" BorderThickness="1" CornerRadius="12" Padding="9,4"><TextBlock x:Name="TopologyStatus" Text="正在检查" FontSize="10.5" FontWeight="SemiBold" Foreground="{DynamicResource BrushSuccess}"/></Border>
                            </Grid>
                            <Grid Grid.Row="1" Margin="0,16,0,14">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="1*"/><ColumnDefinition Width=".7*"/><ColumnDefinition Width="1.15*"/><ColumnDefinition Width=".7*"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="#DFFFFFFF" BorderBrush="{DynamicResource BrushHairline}" BorderThickness="1" CornerRadius="15" Padding="12">
                                    <StackPanel VerticalAlignment="Center"><Border Width="34" Height="34" CornerRadius="17" Background="#100A84FF" HorizontalAlignment="Center" Margin="0,0,0,8"><Path Data="{StaticResource IconPhone}" Width="17" Height="17" Stretch="Uniform" Stroke="{DynamicResource BrushSystemBlue}" StrokeThickness="1.7" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="Android 手机" Style="{StaticResource NodeTitle}"/><TextBlock Text="已配对 · 待发现" Style="{StaticResource NodeMeta}"/></StackPanel>
                                </Border>
                                <Grid Grid.Column="1" VerticalAlignment="Center"><Rectangle Height="2" Fill="{DynamicResource BrushSystemBlue}"/><Border Background="#F4F6FA" CornerRadius="8" Padding="6,3" HorizontalAlignment="Center"><TextBlock Text="Wi-Fi HTTPS" FontSize="9.5" Foreground="{DynamicResource BrushSystemBlue}"/></Border></Grid>
                                <Border x:Name="ReceiverNode" Grid.Column="2" Background="#DFFFFFFF" BorderBrush="{DynamicResource BrushHairline}" BorderThickness="1" CornerRadius="15" Padding="12">
                                    <StackPanel VerticalAlignment="Center"><Border Width="34" Height="34" CornerRadius="17" Background="{DynamicResource BrushGraphite}" HorizontalAlignment="Center" Margin="0,0,0,8"><Path Data="{StaticResource IconComputer}" Width="17" Height="17" Stretch="Uniform" Stroke="White" StrokeThickness="1.7" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="本机接收端" Style="{StaticResource NodeTitle}"/><TextBlock x:Name="TopologyComputerName" Text="当前电脑" Style="{StaticResource NodeMeta}"/></StackPanel>
                                </Border>
                                <Grid Grid.Column="3" VerticalAlignment="Center"><Rectangle Height="2" Fill="{DynamicResource BrushSuccess}"/><Border Background="#F4F6FA" CornerRadius="8" Padding="6,3" HorizontalAlignment="Center"><TextBlock Text="WASAPI" FontSize="9.5" Foreground="{DynamicResource BrushSuccess}"/></Border></Grid>
                                <Border Grid.Column="4" Background="#DFFFFFFF" BorderBrush="{DynamicResource BrushHairline}" BorderThickness="1" CornerRadius="15" Padding="12">
                                    <StackPanel VerticalAlignment="Center"><Border Width="34" Height="34" CornerRadius="17" Background="#102DA44E" HorizontalAlignment="Center" Margin="0,0,0,8"><Path Data="{StaticResource IconMic}" Width="17" Height="17" Stretch="Uniform" Stroke="{DynamicResource BrushSuccess}" StrokeThickness="1.7" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock x:Name="EngineNodeTitle" Text="Typeless" Style="{StaticResource NodeTitle}"/><TextBlock x:Name="EngineNodeMeta" Text="听写会话" Style="{StaticResource NodeMeta}"/></StackPanel>
                                </Border>
                            </Grid>
                            <Border Grid.Row="2" Background="#88FFFFFF" BorderBrush="{DynamicResource BrushHairlineSubtle}" BorderThickness="1" CornerRadius="13" Padding="13,10">
                                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <StackPanel><TextBlock x:Name="CurrentComputerText" Text="当前电脑" Style="{StaticResource TextCardTitle}"/><TextBlock x:Name="ComputerIdText" Text="正在读取电脑身份…" Style="{StaticResource TextFine}" FontFamily="{StaticResource FontFamilyMono}" Margin="0,3,0,0"/></StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"><Border Background="#0F0A84FF" CornerRadius="10" Padding="8,4" Margin="0,0,6,0"><TextBlock Text="secureLan" FontSize="9.5" Foreground="{DynamicResource BrushSystemBlue}"/></Border><Button Content="复制 ID" Style="{StaticResource GlassButton}" Padding="12,6" FontSize="11" Click="CopyId_Click"/></StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>

                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="12"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Border Grid.Row="0" Style="{StaticResource GlassCard}" Padding="20,14">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal"><Border Width="30" Height="30" CornerRadius="15" Background="#102DA44E" Margin="0,0,9,0"><Path Data="{StaticResource IconMic}" Width="15" Height="15" Stretch="Uniform" Stroke="{DynamicResource BrushSuccess}" StrokeThickness="1.7" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="共享麦克风联动" Style="{StaticResource SettingsTitle}" VerticalAlignment="Center"/></StackPanel>
                                    <TextBlock x:Name="SharedMicDetail" Text="关闭 · 开关或 Ctrl+Alt+M 让手机开麦" Style="{StaticResource SettingsMeta}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                                <CheckBox x:Name="SharedMicLinkCheck" Grid.Column="1" Style="{StaticResource SwitchCheckBox}" VerticalAlignment="Center" Checked="SharedMicLink_Changed" Unchecked="SharedMicLink_Changed" AutomationProperties.Name="共享麦克风联动"/>
                            </Grid>
                        </Border>
                        <Border Grid.Row="2" Style="{StaticResource GlassCard}" Padding="20,18">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <StackPanel><TextBlock Text="连接设置" Style="{StaticResource TextSectionTitle}"/><TextBlock Text="保存后会重启接收端" Style="{StaticResource TextFine}" Margin="0,3,0,0"/></StackPanel>
                            <StackPanel Grid.Row="1" Margin="0,14,0,0">
                                <Grid Margin="0,0,0,12"><StackPanel><TextBlock Text="Wi-Fi 自动发现" Style="{StaticResource SettingsTitle}"/><TextBlock Text="同一局域网内应答 UDP 8767" Style="{StaticResource SettingsMeta}"/></StackPanel><CheckBox x:Name="LanDiscoveryCheck" Style="{StaticResource SwitchCheckBox}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid>
                                <Grid Margin="0,0,0,12"><StackPanel><TextBlock Text="USB 自动恢复" Style="{StaticResource SettingsTitle}"/><TextBlock Text="插线时重建 adb reverse" Style="{StaticResource SettingsMeta}"/></StackPanel><CheckBox x:Name="UsbWatchdogCheck" Style="{StaticResource SwitchCheckBox}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid>
                                <Grid Margin="0,0,0,14"><StackPanel><TextBlock Text="登录后启动" Style="{StaticResource SettingsTitle}"/><TextBlock Text="开机打开接收端与本界面" Style="{StaticResource SettingsMeta}"/></StackPanel><CheckBox x:Name="AutoStartCheck" Style="{StaticResource SwitchCheckBox}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid>
                                <Button Content="配置 Agent 操作" Style="{StaticResource GlassButton}" HorizontalContentAlignment="Center" Margin="0,0,0,12" Click="AgentSettings_Click"/>
                                <TextBlock Text="ADB 路径（USB 兜底）" Style="{StaticResource TextFine}" Margin="2,0,0,6"/>
                                <TextBox x:Name="AdbPathBox" Style="{StaticResource GlassTextBox}"/>
                            </StackPanel>
                            <Button Grid.Row="2" Content="保存并应用" Style="{StaticResource BlueButton}" HorizontalContentAlignment="Center" Click="SaveSettings_Click"/>
                        </Grid>
                    </Border>
                    </Grid>
                </Grid>

                <Border Grid.Row="5" Style="{StaticResource GlassCard}" Padding="18,14">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Grid><StackPanel Orientation="Horizontal"><TextBlock Text="运行日志" Style="{StaticResource TextSectionTitle}" FontSize="14"/><TextBlock Text="  ·  仅本机" Style="{StaticResource TextFine}" VerticalAlignment="Center"/></StackPanel><TextBlock Text="本地优先，不经过云端转发" Style="{StaticResource TextFine}" HorizontalAlignment="Right"/></Grid>
                        <RichTextBox x:Name="ActivityLog" Grid.Row="1" Margin="0,8,0,0" Background="#85FFFFFF" BorderThickness="0" IsReadOnly="True" FontFamily="{StaticResource FontFamilyMono}" FontSize="11.5" Foreground="{DynamicResource BrushSlate}" VerticalScrollBarVisibility="Auto" Padding="8,5"/>
                    </Grid>
                </Border>
                </Grid>
            </ScrollViewer>

            <!-- Connection page -->
            <ScrollViewer x:Name="PageConnection" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="48,24,48,40">
                    <TextBlock Text="PHONEDECK / 连接" FontFamily="{StaticResource FontFamilyMono}" FontSize="10.5" FontWeight="Bold" Foreground="{DynamicResource BrushSystemBlue}"/>
                    <TextBlock Text="连接通道" Style="{StaticResource TextPageTitle}" Margin="0,6,0,4"/>
                    <TextBlock Text="日常使用优先走本地 Wi-Fi，首次信任和异常恢复使用 USB。" Style="{StaticResource TextMuted}" Margin="0,0,0,22"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="18"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="28">
                            <StackPanel><Border Width="42" Height="42" CornerRadius="21" Background="#120A84FF" HorizontalAlignment="Left"><Path Data="{StaticResource IconLink}" Width="20" Height="20" Stretch="Uniform" Stroke="{DynamicResource BrushSystemBlue}" StrokeThickness="1.8" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="Wi-Fi 安全通道" Style="{StaticResource TextSectionTitle}" Margin="0,18,0,6"/><TextBlock x:Name="ConnectionWifiStatus" Text="正在检查网络…" Style="{StaticResource TextMuted}"/><TextBlock Text="证书固定 HTTPS · TCP 8766 · UDP 8767 自动发现" Style="{StaticResource TextFine}" Margin="0,8,0,18"/><Button Content="重新检查" Style="{StaticResource BlueButton}" HorizontalAlignment="Left" Click="RefreshButton_Click"/></StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource GlassCard}" Padding="28">
                            <StackPanel><Border Width="42" Height="42" CornerRadius="21" Background="#121D1D1F" HorizontalAlignment="Left"><Path Data="{StaticResource IconPhone}" Width="20" Height="20" Stretch="Uniform" Stroke="{DynamicResource BrushGraphite}" StrokeThickness="1.8" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="USB 兜底通道" Style="{StaticResource TextSectionTitle}" Margin="0,18,0,6"/><TextBlock x:Name="ConnectionUsbStatus" Text="正在检查设备…" Style="{StaticResource TextMuted}"/><TextBlock Text="ADB reverse · 本地 127.0.0.1:8765 · 首次配对" Style="{StaticResource TextFine}" Margin="0,8,0,18"/><Button Content="重新启动接收端" Style="{StaticResource GlassButton}" HorizontalAlignment="Left" Click="RestartButton_Click"/></StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!-- Devices page -->
            <ScrollViewer x:Name="PageDevices" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="48,24,48,40">
                    <TextBlock Text="PHONEDECK / 设备" FontFamily="{StaticResource FontFamilyMono}" FontSize="10.5" FontWeight="Bold" Foreground="{DynamicResource BrushSystemBlue}"/>
                    <TextBlock Text="设备与身份" Style="{StaticResource TextPageTitle}" Margin="0,6,0,4"/>
                    <TextBlock Text="这台电脑的稳定身份、平台能力与当前手机连接状态。" Style="{StaticResource TextMuted}" Margin="0,0,0,22"/>
                    <Button Content="更新所有设备" Style="{StaticResource BlueButton}" HorizontalAlignment="Left" Margin="0,0,0,18" Click="FleetUpdates_Click"/>
                    <Border Style="{StaticResource GlassCard}" Padding="28">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="24"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Border Width="64" Height="64" CornerRadius="32" Background="{DynamicResource BrushGraphite}"><Path Data="{StaticResource IconComputer}" Width="28" Height="28" Stretch="Uniform" Stroke="White" StrokeThickness="1.7" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center"><TextBlock x:Name="DeviceComputerName" Text="当前电脑" Style="{StaticResource TextSectionTitle}"/><TextBlock x:Name="DeviceComputerId" Text="正在读取电脑身份…" Style="{StaticResource TextFine}" FontFamily="{StaticResource FontFamilyMono}" Margin="0,6,0,0"/><StackPanel Orientation="Horizontal" Margin="0,10,0,0"><Border Background="#0F0A84FF" CornerRadius="10" Padding="9,4" Margin="0,0,7,0"><TextBlock Text="windows x64" FontSize="10" Foreground="{DynamicResource BrushSystemBlue}"/></Border><Border Background="#102DA44E" CornerRadius="10" Padding="9,4" Margin="0,0,7,0"><TextBlock Text="phoneAudio" FontSize="10" Foreground="{DynamicResource BrushSuccess}"/></Border><Border Background="#121D1D1F" CornerRadius="10" Padding="9,4"><TextBlock Text="protocol v2" FontSize="10" Foreground="{DynamicResource BrushSlate}"/></Border></StackPanel></StackPanel>
                            <Button Grid.Column="3" Content="复制电脑 ID" Style="{StaticResource GlassButton}" VerticalAlignment="Center" Click="CopyId_Click"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Logs page -->
            <Grid x:Name="PageLogs" Visibility="Collapsed" Margin="48,24,48,40">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="18"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <StackPanel><TextBlock Text="PHONEDECK / 日志" FontFamily="{StaticResource FontFamilyMono}" FontSize="10.5" FontWeight="Bold" Foreground="{DynamicResource BrushSystemBlue}"/><TextBlock Text="运行日志" Style="{StaticResource TextPageTitle}" Margin="0,6,0,4"/><TextBlock Text="接收端启动、网络状态、设置保存和错误记录。" Style="{StaticResource TextMuted}"/></StackPanel>
                <Border Grid.Row="2" Style="{StaticResource GlassCard}" Padding="20"><RichTextBox x:Name="ActivityLogFull" Background="#85FFFFFF" BorderThickness="0" IsReadOnly="True" FontFamily="{StaticResource FontFamilyMono}" FontSize="12" Foreground="{DynamicResource BrushSlate}" VerticalScrollBarVisibility="Auto" Padding="10"/></Border>
            </Grid>

            <!-- Settings page -->
            <ScrollViewer x:Name="PageSettings" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="48,24,48,40" MaxWidth="760" HorizontalAlignment="Center">
                    <TextBlock Text="PHONEDECK / 设置" FontFamily="{StaticResource FontFamilyMono}" FontSize="10.5" FontWeight="Bold" Foreground="{DynamicResource BrushSystemBlue}"/>
                    <TextBlock Text="控制台设置" Style="{StaticResource TextPageTitle}" Margin="0,6,0,4"/>
                    <TextBlock Text="这些设置保存在当前运行目录，保存后自动重启接收端。" Style="{StaticResource TextMuted}" Margin="0,0,0,22"/>
                    <Border Style="{StaticResource GlassCard}" Padding="28">
                        <StackPanel>
                            <Grid Margin="0,0,0,18"><StackPanel><TextBlock Text="Wi-Fi 自动发现" Style="{StaticResource SettingsTitle}"/><TextBlock Text="同一局域网内应答 UDP 8767" Style="{StaticResource SettingsMeta}"/></StackPanel><CheckBox x:Name="LanDiscoveryCheckPage" Style="{StaticResource SwitchCheckBox}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid>
                            <Grid Margin="0,0,0,18"><StackPanel><TextBlock Text="USB 自动恢复" Style="{StaticResource SettingsTitle}"/><TextBlock Text="插线时自动重建 adb reverse" Style="{StaticResource SettingsMeta}"/></StackPanel><CheckBox x:Name="UsbWatchdogCheckPage" Style="{StaticResource SwitchCheckBox}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid>
                            <Grid Margin="0,0,0,22"><StackPanel><TextBlock Text="登录后打开控制台" Style="{StaticResource SettingsTitle}"/><TextBlock Text="Windows 登录后自动启动接收端与本界面" Style="{StaticResource SettingsMeta}"/></StackPanel><CheckBox x:Name="AutoStartCheckPage" Style="{StaticResource SwitchCheckBox}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid>
                            <TextBlock Text="ADB 路径（USB 兜底）" Style="{StaticResource TextFine}" Margin="2,0,0,7"/>
                            <TextBox x:Name="AdbPathBoxPage" Style="{StaticResource GlassTextBox}" Margin="0,0,0,18"/>
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><Button Content="配置 Agent 操作" Style="{StaticResource GlassButton}" HorizontalAlignment="Left" Click="AgentSettings_Click"/><Button Grid.Column="1" Content="保存并应用" Style="{StaticResource BlueButton}" Click="SaveSettingsPage_Click"/></Grid>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource GlassCard}" Padding="28" Margin="0,22,0,0">
                        <StackPanel>
                            <TextBlock Text="语音引擎" Style="{StaticResource TextSectionTitle}"/>
                            <TextBlock Text="手机语音输入使用的电脑端语音转文字软件；随「保存并应用」一起保存并重启接收端。" Style="{StaticResource TextMuted}" Margin="0,6,0,14" TextWrapping="Wrap"/>
                            <WrapPanel x:Name="EngineChipsPanel" Margin="0,0,0,4"/>
                            <TextBlock x:Name="EngineDetailText" Style="{StaticResource TextFine}" TextWrapping="Wrap" Margin="2,2,0,10"/>
                            <StackPanel x:Name="EngineModePanel"/>
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="DataDirectoryText" Style="{StaticResource TextFine}" FontFamily="{StaticResource FontFamilyMono}" TextWrapping="Wrap" Margin="8,14,8,0"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        </Grid>
    </Border>
</Window>
