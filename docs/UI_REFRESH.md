# UI 翻新：简洁原生方向

用户确认：2026-09-14。当前切片：Android 1.6 开发线 UI 预览。
追加反馈：快捷卡片不显示右上角彩色装饰点；保留发送结果反馈与多电脑输入入口。
以下 2026-09-14 切片未包含桌面管理界面；Windows 当前实现以下述 2026-09-21 追加为准，macOS 不变。

第二轮视觉：参考 Raycast 的克制层级与 Stream Deck 的快捷面板结构，页头缩至 19sp，
取消状态卡和常规按钮的重复边框，快捷键使用 72dp 基准高度/12dp 圆角；普通模式隐藏操作说明。
五台及以下电脑使用同屏等分两行按钮（编号 + 当前/在线/离线），更多电脑继续横向滚动。
语音保持 64dp 主按钮、48dp 退格/回车/模式入口；所有电脑身份、配对和动作配置保留。

## 2026-09-21 追加：手机集中设置与轻量桌面

Windows dev.16 / sequence 28，Android dev.21 / versionCode 27。
手机新增电脑设置采用已有浅/深色令牌：设备编号卡片、明确目标、输入法/连接分页、输入法选择卡片、
快捷键编辑、带说明的连接开关和固定保存区。切页保留草稿；刷新/返回提示放弃未保存内容。
成功仅更新保存分组的 revision，失败和冲突不清除草稿。横屏将目标移到页头，减少重复摘要；大字体允许滚动。

Windows 使用 Luma 环形品牌图、原生状态卡片、电脑/输入法/音频/版本明细、重连按钮及手机设置路径。
浅/深色跟随打开窗口时的系统应用主题。仅使用 WinForms、简单圆角填充和现有本地图标；
保持关闭后释放窗口、托盘不刷新隐藏 UI 的生命周期，不引入浏览器或动画循环。

验证：Windows 四种浅/深色 × 就绪/离线布局通过 150% DPI 文字边界检查、10 次创建关闭后 Form 释放。
Samsung 临时独立验收包通过浅/深色、150% 字体、横屏、失败状态和分页草稿/409 冲突验证；
示例设备和受控响应明确标注，测试包及专用反向端口已清理，原应用和配对未动。
正式 Debug/Preview APK 检查不含验收 Instrumentation。91 项 Windows 与 14 项 Android 测试通过，
lint 0 error / 44 warning。桌面已签名更新；手机用户现用预览包原签名不可用，正式覆盖仍待通道选择。

本地截图和隔离探针位于 ignored `outputs/ui-upgrade/desktop`、`outputs/ui-upgrade/android/verified-final`，
最终签名包为 `outputs/ui-upgrade/PhoneDeck-28.zip`。UI 托盘私有提交约 14 MiB；其工作集与接收服务另计，
完整测量口径见 [手机统一设置](PHONE_MANAGED_DESKTOP.md)。

## 原手机主页面信息结构

- 顶部固定 PhoneDeck、设置、当前电脑全名和独立连接摘要。点击连接卡打开所有电脑；刷新按钮只做重新探测。
- 中部为可滚动三列纯文字快捷键，名称优先，键位为次级信息；保留编辑、排序、连续按键与确认反馈。
- 底部保留语音模式、主按钮、退格/回车、电平、结果、采音状态和目标编号。
  使用真实测量高度而非 362dp 占位。大字体下底部区域独立滚动，横屏双栏。
- 设置分为使用偏好、连接与维护；按用户追加要求下线九套经典主题，只提供浅色与深色。
- 设备列表/切换继续使用已有 computerId 和健康状态；离线不误发，长按可以管理旧条目。

## 设计令牌

| 语义 | 简洁浅色 | 简洁深色 |
|---|---|---|
| 背景 | #F0F2F5 | #121316 |
| 表面 | #FFFFFF | #1B1E24 |
| 主文字 | #18202D | #F0F2F6 |
| 次级文字 | #626D7C | #A7B0C0 |
| 强调 | #285ED4 | #A9C7FF |
| 细边 | #E1E5EB | #343B48 |

原生系统字体；快捷卡片 12dp 圆角，20dp 页面边距，8dp 卡片间距。
主要触摸入口至少 48dp。状态使用文字与颜色双重反馈。新主题无渐变、背景光斑或投影。
功能线性图形由 DeckIconView 原创绘制，不引入字体、图片、网络资产或第三方许可依赖。
Android 原生深色窗口主题同时作用于对话框、开关和输入控件。

## 数据与构建边界

- 历史主题 ID 自动迁移至简洁浅/深色；快捷键 schemaVersion=1、配对、语音配置均保留。
- 主题变化只重绑主界面视图，不 recreate 接收/采音状态所有者。
- `UiText` 仅消除前台应用摘要中的缺失/null 展示，不修改协议或电脑名称。
- `:app:assembleUiPreview` 生成 `com.codex.phonedeck.preview`，标签“PhoneDeck UI 预览”。
  独立数据，可与原版并存；默认开发签名，禁止用于 fleet 更新或冒充原渠道升级。
- Preview 运行的仍是真实业务代码；没有伪在线设备或伪识别结果。连接与麦克风操作仍需真实配对和用户授权。

## 验证清单

开发构建、Debug 单测、Lint、UI Preview 构建；浅色/深色首页和全部设置子页，编辑/按键选择/更新页面；
滚动时电脑状态与语音区不丢失；字体放大、横屏、长设备名、离线与空状态；
主题切换后的会话、点击/按住、翻译/问答、20 轮语音真实识别另列硬件验收。
实际执行结果写入 HANDOFF，未执行项目不得标为通过。
